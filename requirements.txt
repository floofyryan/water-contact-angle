# Water Contact Angle Analyzer — Python dependencies
# Install with:  pip install -r requirements.txt
# Build .exe:    pip install pyinstaller && pyinstaller --onefile --windowed app.py

# Core scientific stack
numpy>=1.24
opencv-python>=4.7
matplotlib>=3.7
scipy>=1.10          # optional — not currently used directly, but good to have for future fitting
typing_extensions>=4.0  # required by numpy, matplotlib, scipy on Python < 3.12

# GUI (tkinter is bundled with Python — no pip install needed)
# Pillow is required by some matplotlib backends on Windows
Pillow>=9.0

# Drag-and-drop is implemented via ctypes (built-in) — no extra package needed
